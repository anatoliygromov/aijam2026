# Team 3 assets — Design Team · CSS 3D transforms

Drop any **local** files this page needs here (card art, images, etc.).
Reference them from `team.html` with a relative path, e.g. `assets/team3/card-front.png`.

You don't have to use this folder: you can also load assets straight from a URL/CDN
or use AI-generated image URLs / data-URIs. If you don't need local files, ignore this folder.

Upload files here with **Add file → Upload files** on GitHub — no terminal needed.

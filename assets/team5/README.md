# Team 5 assets — 404 · GLSL fragment shader

Drop any **local** files this page needs here (textures, images).
Reference them from `404.html` with a relative path, e.g. `assets/team5/noise.png`.

You don't have to use this folder: a fragment shader is usually 100% procedural (zero files),
or you can load a texture from a URL/CDN. If you don't need local files, ignore this folder.

Upload files here with **Add file → Upload files** on GitHub — no terminal needed.
